# Prometheus Canon Staging Checksums — Reference Note

This directory contains a copied reference checksum register from the canonical staging context.

It is retained as provenance evidence only.

It is not a root-level checksum register for this founder mirror repository.

Because the copied checksum file contains paths relative to the canonical repository root, it must not be verified in place from this `docs/` directory.

The active founder mirror staging checksum register is:

`FOUNDER_MIRROR_STAGING_CHECKSUMS.txt`

Final canonical checksums must be regenerated only after final QA, textual freeze, release manifest update, and authorized signing.
