# Reproducible Release — Superseded Mirror Procedure

This repository does not independently build or authorize canonical releases.

The earlier local extraction and signing procedure is retained in Git history but is no longer controlling for the founder mirror.

Use:

`MIRROR_REPRODUCIBILITY_AND_VERIFICATION.md`

The controlling flow is:

canonical source → canonical release → manifest/signature verification → byte-for-byte mirror publication.
